# Description: Database controller class

# Importing the necessary libraries
import os
import psycopg2

# ? Defining the class DatabaseController
class DatabaseController:
    """ * A class that represents the database controller """